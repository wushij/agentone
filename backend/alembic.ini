# AgentOne Alembic config -- database migrations entry
# Usage:
#   cd backend
#   alembic revision -m "add xxx table"   # create new migration
#   alembic upgrade head                  # apply migrations
#   alembic stamp head                    # adopt an existing DB as baseline (run once)

[alembic]
script_location = alembic
prepend_sys_path = .
# sqlalchemy.url is empty: env.py reads DATABASE_URL from app.config.settings at runtime
sqlalchemy.url =

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
